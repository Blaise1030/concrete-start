import {type RouteSectionProps} from "@solidjs/router";

export default function Login(props: RouteSectionProps) {
  return (
    <main>
      <div class="bg-red-500"> This is </div>
    </main>
  );
}
